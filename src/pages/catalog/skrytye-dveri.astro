---
import BaseLayout from '../../layouts/BaseLayout.astro'
import Breadcrumbs from '../../components/ui/Breadcrumbs.astro'
import FaqAccordion from '../../components/ui/FaqAccordion.astro'
import CoverSwitcher      from '../../components/catalog/CoverSwitcher.vue'
import ColorSelector      from '../../components/catalog/ColorSelector.vue'
import InfoImageViewer    from '../../components/catalog/InfoImageViewer.vue'
import WorksGallery      from '../../components/catalog/WorksGallery.vue'
import PromoBanner       from '../../components/ui/PromoBanner.vue'
import PhotoAutoplaySlider from '../../components/ui/PhotoAutoplaySlider.vue'
import { INVISIBLE_WORKS } from '../../data/skrytye-dveri-works'
import { PROMOS } from '../../data/promos'
import {
  IMAGES,
  INFO_IMAGES,
  BOX_PRO_IMAGE,
  BOX_LITE_IMAGE,
  SECRET_SIZES,
  SECRET_REVERS_SIZES,
  DOOR_HEIGHT,
  SECRET_PRICES_DISPLAY,
  SECRET_MIN_BLADE_PRICE,
  SECRET_MIN_KIT_PRICE,
  SECRET_MIN_BLADE_PRICE_ORIGINAL,
  SECRET_MIN_KIT_PRICE_ORIGINAL,
  SECRET_REVERS_MIN_BLADE_PRICE,
  SECRET_REVERS_MIN_KIT_PRICE,
  SECRET_REVERS_MIN_BLADE_PRICE_ORIGINAL,
  SECRET_REVERS_MIN_KIT_PRICE_ORIGINAL,
  SECRET_PROMO_ACTIVE,
  SECRET_PROMO_VALID_UNTIL,
  CUSTOM_HEIGHT_TIERS,
  CUSTOM_LEAD_TIME,
  calcCustomPrice,
  REFLEX_IMAGE,
  REFLEX_OPENING_DIAGRAM,
  REFLEX_PRICES,
  REFLEX_MIN_BLADE_PRICE,
  REFLEX_MIN_KIT_PRICE,
  REFLEX_REVERS_MIN_KIT_PRICE,
  REFLEX_HEIGHT_TIERS,
  REFLEX_TWO_SIDED_MIRROR_SURCHARGE,
} from '../../data/skrytye-dveri-products'

const secretPromo = PROMOS.find(p => p.id === 3)

const REFLEX_CDN = 'https://storage.yandexcloud.net/vfd74ru/invisible/'
const REFLEX_IMAGES = [
  `${REFLEX_CDN}2213EA7D-2E12-4D33-9BC3-63E4943E0098.webp`,
  `${REFLEX_CDN}A158F8FC-901B-4262-BDF6-D68E573300C8.webp`,
  `${REFLEX_CDN}41CFB5EC-5142-462A-A702-E9AECDE149AD.webp`,
  REFLEX_IMAGE,
]

const PHONE = '+79000297888'
const TG    = 'https://t.me/vfddoors74'
const VK    = 'https://vk.com/vfddoors74'
const MAX   = 'https://max.ru/id452402308842_biz'

const WORKS_LIMIT = 4
const ARCHIVE_HREF = '/catalog/skrytye-dveri/raboty/'
const featuredWorks = INVISIBLE_WORKS.slice(0, WORKS_LIMIT)

const fmt = (n: number) => n.toLocaleString('ru-RU') + ' ₽'

const canonicalUrl = Astro.site
  ? new URL('/catalog/skrytye-dveri/', Astro.site).href
  : undefined

const breadcrumbItems = [
  { label: 'Главная', href: '/' },
  { label: 'Каталог', href: '/catalog/' },
  { label: 'Скрытые двери' },
]

const faq = [
  { q: 'Чем скрытая дверь отличается от обычной?',
    a: 'Нет видимых наличников и коробки — алюминиевая рама монтируется заподлицо, полотно располагается вровень со стеной. Петли скрытые. Дверь визуально растворяется в интерьере.' },
  { q: 'Что входит в комплект?',
    a: 'Полотно серии «Секрет» или «Секрет Реверс» и комплект скрытого короба: профиль 54×43 мм (2,5 пог. м), запил под ответную планку, ответная планка и две скрытые петли. Ручка, стопор, завёртка WC — опционально.' },
  { q: 'Можно покрасить в цвет стены?',
    a: 'Да. Полотно поставляется в грунте — красится или штукатурится после монтажа. Главное: красить только после установки, чтобы зазоры совпали по цвету.' },
  { q: 'В чём разница «Секрет» и «Секрет Реверс»?',
    a: '«Секрет» открывается «на себя» (прямой монтаж). «Секрет Реверс» — «от себя», в сторону соседнего помещения. Реверс удобен там, где нежелательно, чтобы полотно занимало пространство в комнате.' },
  { q: 'Как определить сторонность — левая дверь или правая?',
    a: 'Встаньте лицом к двери снаружи помещения (со стороны, где открываете). Если петли справа — дверь правая, петли слева — левая. Правило одинаково для «Секрет» и «Секрет Реверс». Замерщик ВФД уточнит сторонность при выезде.' },
  { q: 'Какой цвет алюминиевой кромки выбрать?',
    a: 'Три варианта: чёрная (анодированный алюминий) — под тёмную фурнитуру и контрастный интерьер; серебро (натуральный алюминий) — нейтральный, для светлых стен; золото (золотистый анод) — под бронзу, латунь и арт-деко. Цена одинакова для всех трёх.' },
  { q: 'Какой нужен проём?',
    a: 'Проём должен быть ровным, откосы — вровень с плоскостью стены. Минимальная толщина стены — 90 мм. Замерщик выезжает бесплатно и даёт конкретные рекомендации.' },
  { q: 'Есть нестандартные размеры?',
    a: `Стандарт в наличии: ширины ${SECRET_SIZES.join(', ')} мм, высота 2000 мм. Нестандартные высоты под заказ — от 2050 до 2700 мм, надбавка к цене полотна от +20% до +50%. Срок изготовления: ${CUSTOM_LEAD_TIME}.` },
  { q: 'Сколько стоит установка?',
    a: 'Стоимость монтажа рассчитывается отдельно. Мы работаем по Челябинску, даём гарантию на работы. Уточняйте у менеджера при заказе.' },
  { q: 'Чем «Рефлекс» отличается от «Секрета»?',
    a: 'Тем же скрытым монтажом вровень со стеной, но вместо глухого полотна — зеркало во всю высоту двери. Изготавливается индивидуально, высотой до 2,5 м, доступны прямое и обратное открывание.' },
]

const structuredData = canonicalUrl ? [
  {
    '@context': 'https://schema.org',
    '@type': 'BreadcrumbList',
    itemListElement: breadcrumbItems.map((item, i) => ({
      '@type': 'ListItem',
      position: i + 1,
      name: item.label,
      ...(item.href ? { item: new URL(item.href, canonicalUrl).href } : {}),
    })),
  },
  {
    '@context': 'https://schema.org',
    '@type': 'FAQPage',
    mainEntity: faq.map(f => ({
      '@type': 'Question',
      name: f.q,
      acceptedAnswer: { '@type': 'Answer', text: f.a },
    })),
  },
  ...(featuredWorks.length > 0 ? [{
    '@context': 'https://schema.org',
    '@type': 'ItemList',
    name: 'Наши работы — монтаж скрытых дверей в Челябинске и Челябинской области',
    itemListElement: featuredWorks.map((w, i) => ({
      '@type': 'ListItem',
      position: i + 1,
      item: {
        '@type': 'CreativeWork',
        name: w.title,
        ...(w.story ? { description: w.story } : {}),
        dateCreated: w.date,
        image: w.images,
        ...(w.location ? { locationCreated: { '@type': 'Place', name: w.location } } : {}),
      },
    })),
  }] : []),
  {
    '@context': 'https://schema.org',
    '@type': 'Product',
    name: 'Скрытые двери — серии «Секрет» и «Секрет Реверс»',
    description: 'Двери скрытого монтажа: полотно заподлицо со стеной, алюминиевый короб, скрытые петли. Под покраску, штукатурку, обои. Кромка: чёрная, серебро, золото.',
    brand: { '@type': 'Brand', name: 'ВФД' },
    image: IMAGES.invisible,
    offers: {
      '@type': 'AggregateOffer',
      priceCurrency: 'RUB',
      lowPrice: SECRET_MIN_KIT_PRICE,
      highPrice: Math.max(...SECRET_PRICES_DISPLAY.map(r => r.kitStandardPrice)),
      availability: 'https://schema.org/InStock',
      areaServed: [
        { '@type': 'City', name: 'Челябинск' },
        { '@type': 'AdministrativeArea', name: 'Челябинская область' },
      ],
    },
  },
  {
    '@context': 'https://schema.org',
    '@type': 'Product',
    name: 'Скрытая дверь с зеркалом «Рефлекс»',
    description: 'Скрытая дверь с зеркальным полотном во всю высоту — устанавливается вровень со стеной, изготавливается индивидуально высотой до 2,5 м. Кромка: чёрная, серебро, золото.',
    brand: { '@type': 'Brand', name: 'ВФД' },
    image: REFLEX_IMAGE,
    offers: {
      '@type': 'AggregateOffer',
      priceCurrency: 'RUB',
      lowPrice: REFLEX_MIN_KIT_PRICE,
      highPrice: Math.max(...REFLEX_PRICES.map(r => r.kitStandardPrice)),
      availability: 'https://schema.org/InStock',
      areaServed: [
        { '@type': 'City', name: 'Челябинск' },
        { '@type': 'AdministrativeArea', name: 'Челябинская область' },
      ],
    },
  },
] : undefined
---

<BaseLayout
  title="Скрытые двери в Челябинске — цены и монтаж | ВФД"
  description={`Скрытые двери «Секрет» и «Секрет Реверс» в Челябинске — полотно вровень со стеной, без наличников и видимых петель. Комплект от ${fmt(SECRET_MIN_KIT_PRICE)}. Бесплатный замер, монтаж под ключ.`}
  ogImage={IMAGES.invisible}
  preloadImage={IMAGES.covers[0].src}
  canonical={canonicalUrl}
  structuredData={structuredData}
>

  {secretPromo && (
    <PromoBanner client:only="vue" promo={secretPromo} anchor="secret-prices" />
  )}

  <div class="breadcrumb-bar">
    <div class="container">
      <Breadcrumbs items={breadcrumbItems} />
    </div>
  </div>

  <!-- ══════════════════════════════════════════════
       HERO
  ══════════════════════════════════════════════ -->
  <div class="container sd-hero-wrap">
    <section class="sd-hero" aria-label="Двери скрытого монтажа">
      <CoverSwitcher client:load />

      <div class="sd-hero__body">
        <p class="sd-eyebrow">Скрытый монтаж · Челябинск</p>
        <h1 class="sd-hero__h1">Двери скрытого<br/>монтажа</h1>
        <p class="sd-hero__lead">
          Полотно вровень со стеной. Без наличников,
          без видимых петель. Алюминиевый короб прячется
          в проём — снаружи только чистая плоскость.
        </p>
        <div class="sd-hero__actions">
          <a href={TG} target="_blank" rel="noopener" class="btn btn-ghost">Получить расчёт</a>
          <a href={`tel:${PHONE}`} class="btn btn-ghost">Позвонить</a>
        </div>
      </div>
    </section>
  </div>

  <!-- ══════════════════════════════════════════════
       СЕРИИ — 2 ПРОДУКТА
  ══════════════════════════════════════════════ -->
  <section class="section">
    <div class="container">
      <div class="sd-products">

        <!-- Секрет -->
        <article class="sd-product">
          <div class="sd-product__photo">
            <img
              src={IMAGES.invisible}
              alt="Скрытая дверь серии «Секрет» — прямой монтаж"
              loading="lazy"
              decoding="async"
              width="800"
              height="1000"
            />
          </div>
          <div class="sd-product__info">
            <span class="sd-product__eyebrow">Серия</span>
            <h2 class="sd-product__name">«Секрет»</h2>
            <p class="sd-product__type">Скрытый монтаж</p>
            <p class="sd-product__desc">
              Классический скрытый монтаж: дверь открывается «на себя».
              Алюминиевый короб монтируется заподлицо, полотно в грунте —
              под покраску, штукатурку или оклейку обоями в цвет стен.
            </p>
            <div class="sd-product__specs">
              <div class="sd-spec">
                <span class="sd-spec__key">Ширины в наличии</span>
                <span class="sd-spec__val">{SECRET_SIZES.map(w => `${w} мм`).join(' · ')}</span>
              </div>
              <div class="sd-spec">
                <span class="sd-spec__key">Высота стандарт</span>
                <span class="sd-spec__val">{DOOR_HEIGHT} мм</span>
              </div>
              <div class="sd-spec">
                <span class="sd-spec__key">Направление</span>
                <span class="sd-spec__val">Левая · Правая</span>
              </div>
              <div class="sd-spec">
                <span class="sd-spec__key">Цвет кромки</span>
                <span class="sd-spec__val">Чёрная · Серебро · Золото</span>
              </div>
            </div>
            {SECRET_PROMO_ACTIVE && (
              <span class="sd-promo-chip">🔥 Акция -8% до 31 августа</span>
            )}
            <div class="sd-product__price">
              <div>
                <span class="sd-price-label">Полотно</span>
                <span>
                  {SECRET_PROMO_ACTIVE && <span class="sd-price-old">{fmt(SECRET_MIN_BLADE_PRICE_ORIGINAL)}</span>}
                  <span class="sd-price-val">от {fmt(SECRET_MIN_BLADE_PRICE)}</span>
                </span>
              </div>
              <div>
                <span class="sd-price-label">Комплект с коробом</span>
                <span>
                  {SECRET_PROMO_ACTIVE && <span class="sd-price-old">{fmt(SECRET_MIN_KIT_PRICE_ORIGINAL)}</span>}
                  <span class="sd-price-val sd-price-val--accent">от {fmt(SECRET_MIN_KIT_PRICE)}</span>
                </span>
              </div>
            </div>
            <a href={TG} target="_blank" rel="noopener" class="btn btn-primary sd-product__btn">
              Рассчитать стоимость
            </a>
          </div>
        </article>

        <!-- Секрет Реверс -->
        <article class="sd-product sd-product--rev">
          <div class="sd-product__photo">
            <img
              src={IMAGES.invisibleRev}
              alt="Скрытая дверь серии «Секрет Реверс» — реверсивный монтаж"
              loading="lazy"
              decoding="async"
              width="800"
              height="1000"
            />
          </div>
          <div class="sd-product__info">
            <span class="sd-product__eyebrow">Серия</span>
            <h2 class="sd-product__name">«Секрет Реверс»</h2>
            <p class="sd-product__type">Реверсивный монтаж</p>
            <p class="sd-product__desc">
              Дверь открывается «от себя» — в сторону соседнего помещения.
              Удобно, когда нежелательно, чтобы полотно занимало пространство
              в текущей комнате. При заказе указывается сторона петель.
            </p>
            <div class="sd-product__specs">
              <div class="sd-spec">
                <span class="sd-spec__key">Ширины в наличии</span>
                <span class="sd-spec__val">{SECRET_REVERS_SIZES.map(w => `${w} мм`).join(' · ')}</span>
              </div>
              <div class="sd-spec">
                <span class="sd-spec__key">Высота стандарт</span>
                <span class="sd-spec__val">{DOOR_HEIGHT} мм</span>
              </div>
              <div class="sd-spec">
                <span class="sd-spec__key">Направление</span>
                <span class="sd-spec__val">Левая · Правая</span>
              </div>
              <div class="sd-spec">
                <span class="sd-spec__key">Цвет кромки</span>
                <span class="sd-spec__val">Чёрная · Серебро · Золото</span>
              </div>
            </div>
            {SECRET_PROMO_ACTIVE && (
              <span class="sd-promo-chip">🔥 Акция -8% до 31 августа</span>
            )}
            <div class="sd-product__price">
              <div>
                <span class="sd-price-label">Полотно</span>
                <span>
                  {SECRET_PROMO_ACTIVE && <span class="sd-price-old">{fmt(SECRET_REVERS_MIN_BLADE_PRICE_ORIGINAL)}</span>}
                  <span class="sd-price-val">от {fmt(SECRET_REVERS_MIN_BLADE_PRICE)}</span>
                </span>
              </div>
              <div>
                <span class="sd-price-label">Комплект с коробом</span>
                <span>
                  {SECRET_PROMO_ACTIVE && <span class="sd-price-old">{fmt(SECRET_REVERS_MIN_KIT_PRICE_ORIGINAL)}</span>}
                  <span class="sd-price-val sd-price-val--accent">от {fmt(SECRET_REVERS_MIN_KIT_PRICE)}</span>
                </span>
              </div>
            </div>
            <a href={TG} target="_blank" rel="noopener" class="btn btn-primary sd-product__btn">
              Рассчитать стоимость
            </a>
          </div>
        </article>

      </div>

      <!-- Цены «Секрет» / «Секрет Реверс» по кромке и стороне открывания -->
      <h3 id="secret-prices" class="sd-h3" style="margin-top: 2.5rem">Цены «Секрет» по кромке и стороне открывания</h3>
      {SECRET_PROMO_ACTIVE && (
        <p class="sd-promo-note">
          🔥 Цены в таблице уже с учётом акции -8% — действует до 31 августа 2026 г.
        </p>
      )}
      <div class="sd-table-wrap sd-stack-table-wrap">
        <table class="sd-compare sd-stack-table">
          <thead>
            <tr>
              <th scope="col">Кромка</th>
              <th scope="col">Открывание</th>
              <th scope="col">Полотно</th>
              <th scope="col">Комплект Стандарт</th>
              <th scope="col">Комплект Лайт</th>
            </tr>
          </thead>
          <tbody>
            {SECRET_PRICES_DISPLAY.map(row => (
              <tr>
                <td data-label="Кромка">{row.edgeLabel}</td>
                <td data-label="Открывание">{row.reverse ? 'Обратное' : 'Прямое'}</td>
                <td data-label="Полотно">{fmt(row.bladePrice)}</td>
                <td data-label="Комплект Стандарт"><strong>{fmt(row.kitStandardPrice)}</strong></td>
                <td data-label="Комплект Лайт">{row.kitLitePrice != null ? fmt(row.kitLitePrice) : '—'}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
      <p class="sd-table-foot">
        Комплект Стандарт и Комплект Лайт отличаются коробом — разница в конструкции ниже.
        Для кромки золото комплект Лайт не предлагается.
      </p>

      <!-- Короб «Секрет» — два варианта конструкции, разрезы кликабельны (модалка) -->
      <h3 class="sd-h3" style="margin-top: 2.5rem">Короб «Секрет»: Стандарт и Лайт — в чём разница</h3>
      <InfoImageViewer
        client:only="vue"
        variant="wide"
        aspectRatio="5 / 4"
        images={[
          { src: BOX_LITE_IMAGE, caption: 'Короб «Секрет Лайт» — разрез', alt: 'Разрез короба «Секрет Лайт»: алюминиевый профиль с наполнением из массива сосны' },
          { src: BOX_PRO_IMAGE,  caption: 'Короб «Секрет» — профиль',     alt: 'Профиль короба «Секрет»: монолитный алюминиевый сплав со штукатурным крылом' },
        ]}
      />
      <div class="sd-box-types">
        <div class="sd-box-type">
          <h4 class="sd-box-type__name">Короб «Секрет Лайт ВФД»</h4>
          <p class="sd-p">
            Облегчённый комплект скрытого алюминиевого короба, разработанный для монтажа
            межкомнатных дверей-невидимок. Конструкция позволяет установить дверное полотно
            в одной плоскости со стеной, полностью отказавшись от традиционных наличников
            и создавая эффект «двери без двери».
          </p>
          <div class="sd-box-feature">
            <h5>Облегчённый профиль с наполнением из массива сосны</h5>
            <p>
              Сечение короба оптимизировано для снижения массы без потери жёсткости,
              необходимой для стандартных межкомнатных полотен. Внешний контур выполнен
              из тонкостенного алюминиевого профиля, внутри которого расположен массив
              сосны. Такое сочетание решает сразу несколько задач: алюминий задаёт точную
              геометрию и защищает конструкцию от внешних воздействий, а древесина
              обеспечивает плотную фиксацию крепежа.
            </p>
          </div>
        </div>
        <div class="sd-box-type">
          <h4 class="sd-box-type__name">Короб «Секрет»</h4>
          <p class="sd-p">
            Система скрытой установки межкомнатных дверей, выполненная из экструдированного
            алюминиевого сплава. Конструкция монтируется вровень со стеной и формирует
            безупречно ровный проём без наличников, создавая эффект «двери-невидимки».
          </p>
          <div class="sd-box-feature">
            <h5>Монолитный профиль</h5>
            <p>
              Короб выполнен из единого алюминиевого сплава методом экструзии. Отсутствие
              составных элементов и клеевых соединений исключает расслоение, деформацию
              и образование микрощелей на стыках.
            </p>
          </div>
          <div class="sd-box-feature">
            <h5>Интегрированное штукатурное крыло</h5>
            <p>
              Профиль оснащён монтажной полкой с рифлением, которая обеспечивает надёжную
              адгезию со штукатуркой, шпаклёвкой и армирующей сеткой. Переход от короба
              к стене получается бесшовным и не даёт трещин при усадке.
            </p>
          </div>
          <div class="sd-box-feature">
            <h5>Усиленный узел притвора</h5>
            <p>
              Внутренняя геометрия профиля рассчитана под установку магнитных и механических
              замков скрытого типа без дополнительного фрезерования.
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════════════
       «РЕФЛЕКС» — СКРЫТАЯ ДВЕРЬ С ЗЕРКАЛОМ
  ══════════════════════════════════════════════ -->
  <section class="section" id="reflex" style="padding-top: 0">
    <div class="container">
      <div class="sd-products">

        <article class="sd-product">
          <div class="sd-product__photo">
            <PhotoAutoplaySlider
              client:visible
              images={REFLEX_IMAGES}
              alt="Скрытая дверь с зеркалом «Рефлекс» — полотно заподлицо со стеной"
            />
          </div>
          <div class="sd-product__info">
            <span class="sd-product__eyebrow">Новинка</span>
            <h2 class="sd-product__name">«Рефлекс»</h2>
            <p class="sd-product__type">Скрытый монтаж, зеркальное полотно</p>
            <p class="sd-product__desc">
              Дизайнерское решение для интерьера: зеркало во всю высоту полотна визуально
              увеличивает пространство и добавляет комнате естественного света. Дверь
              по-прежнему устанавливается вровень со стеной — тот же эффект скрытности,
              что и у «Секрета». Изготавливается по индивидуальным размерам, высотой до 2,5 м.
            </p>
            <div class="sd-product__specs">
              <div class="sd-spec">
                <span class="sd-spec__key">Монтаж</span>
                <span class="sd-spec__val">Прямой · Обратный</span>
              </div>
              <div class="sd-spec">
                <span class="sd-spec__key">Высота</span>
                <span class="sd-spec__val">Индивидуально, до 2500 мм</span>
              </div>
              <div class="sd-spec">
                <span class="sd-spec__key">Полотно</span>
                <span class="sd-spec__val">Финиш-грунт + зеркало</span>
              </div>
              <div class="sd-spec">
                <span class="sd-spec__key">Цвет кромки</span>
                <span class="sd-spec__val">Чёрная · Серебро · Золото</span>
              </div>
            </div>
            <div class="sd-product__price">
              <div>
                <span class="sd-price-label">Полотно</span>
                <span class="sd-price-val">от {fmt(REFLEX_MIN_BLADE_PRICE)}</span>
              </div>
              <div>
                <span class="sd-price-label">Комплект с коробом</span>
                <span class="sd-price-val sd-price-val--accent">от {fmt(REFLEX_MIN_KIT_PRICE)}</span>
              </div>
              <div>
                <span class="sd-price-label">Комплект, зеркало с двух сторон</span>
                <span class="sd-price-val">от {fmt(REFLEX_MIN_KIT_PRICE + REFLEX_TWO_SIDED_MIRROR_SURCHARGE)}</span>
              </div>
            </div>
            <a href={TG} target="_blank" rel="noopener" class="btn btn-primary sd-product__btn">
              Рассчитать стоимость
            </a>
          </div>
        </article>

      </div>

      <!-- Цены «Рефлекс» по кромке и стороне открывания -->
      <h3 class="sd-h3" style="margin-top: 2.5rem">Цены «Рефлекс» по кромке и стороне открывания</h3>
      <div class="sd-table-wrap sd-stack-table-wrap">
        <table class="sd-compare sd-stack-table">
          <thead>
            <tr>
              <th scope="col">Кромка</th>
              <th scope="col">Открывание</th>
              <th scope="col">Полотно</th>
              <th scope="col">Комплект Стандарт</th>
              <th scope="col">Комплект Лайт</th>
              <th scope="col">Стандарт, зеркало с обеих сторон</th>
              <th scope="col">Лайт, зеркало с обеих сторон</th>
            </tr>
          </thead>
          <tbody>
            {REFLEX_PRICES.map(row => (
              <tr>
                <td data-label="Кромка">{row.edgeLabel}</td>
                <td data-label="Открывание">{row.reverse ? 'Обратное' : 'Прямое'}</td>
                <td data-label="Полотно">{fmt(row.bladePrice)}</td>
                <td data-label="Комплект Стандарт"><strong>{fmt(row.kitStandardPrice)}</strong></td>
                <td data-label="Комплект Лайт">{row.kitLitePrice != null ? fmt(row.kitLitePrice) : '—'}</td>
                <td data-label="Стандарт, зеркало с обеих сторон"><strong>{fmt(row.kitStandardPrice + REFLEX_TWO_SIDED_MIRROR_SURCHARGE)}</strong></td>
                <td data-label="Лайт, зеркало с обеих сторон">{row.kitLitePrice != null ? fmt(row.kitLitePrice + REFLEX_TWO_SIDED_MIRROR_SURCHARGE) : '—'}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
      <p class="sd-table-foot">
        Комплект Стандарт и Комплект Лайт отличаются комплектацией короба — уточняйте у менеджера.
        Для кромки золото комплект Лайт не предлагается.
      </p>

      <!-- Схема определения стороны открывания — свой экземпляр InfoImageViewer:
           client:only="vue", тот же безопасный паттерн, что уже используется
           выше на этой странице (не хайдрейтим Teleport-модалку через SSR). -->
      <InfoImageViewer
        client:only="vue"
        variant="wide"
        aspectRatio="16 / 9"
        images={[
          { src: REFLEX_OPENING_DIAGRAM, caption: 'Определение стороны открывания', alt: 'Схема определения стороны открывания для скрытой двери с зеркалом' },
        ]}
      />

      <!-- Нестандартные высоты «Рефлекс» — тот же расчёт, что у «Секрет»,
           но верхний тир урезан: максимум по «Рефлекс» — 2500 мм, не 2700. -->
      <div class="sd-custom-section">
        <h3 class="sd-h3">Нестандартные высоты — под заказ</h3>
        <p class="sd-p" style="margin-bottom: 1rem">
          Надбавка применяется к полотну и коробу. Максимальная высота — 2500 мм.
          Срок изготовления: {CUSTOM_LEAD_TIME}.
        </p>
        <div class="sd-table-wrap sd-stack-table-wrap">
          <table class="sd-compare sd-stack-table">
            <thead>
              <tr>
                <th scope="col">Высота</th>
                <th scope="col">Надбавка</th>
                <th scope="col">«Рефлекс», прямое</th>
                <th scope="col">«Рефлекс», обратное</th>
              </tr>
            </thead>
            <tbody>
              {REFLEX_HEIGHT_TIERS.map(tier => (
                <tr>
                  <th scope="row">{tier.range} мм</th>
                  <td class="sd-custom__pct" data-label="Надбавка">{tier.pct}</td>
                  <td data-label="«Рефлекс», прямое">{fmt(calcCustomPrice(REFLEX_MIN_KIT_PRICE, tier.surcharge))}</td>
                  <td data-label="«Рефлекс», обратное">{fmt(calcCustomPrice(REFLEX_REVERS_MIN_KIT_PRICE, tier.surcharge))}</td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
        <p class="sd-table-foot" style="margin-top: 0.75rem">
          Цена включает полотно и короб с той же надбавкой. Расчёт — от минимального
          комплекта (Лайт, где предлагается) для соответствующего направления.
        </p>
      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════════════
       ТАБЛИЦА ЦЕН И РАЗМЕРОВ
  ══════════════════════════════════════════════ -->
  <section class="section" style="padding-top: 0">
    <div class="container">
      <h2 class="sd-h2">Цены и размеры</h2>

      <div class="sd-table-wrap">
        <table class="sd-compare">
          <thead>
            <tr>
              <th scope="col" class="sd-compare__row-head"></th>
              <th scope="col">«Секрет»</th>
              <th scope="col">«Секрет Реверс»</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <th scope="row">Монтаж</th>
              <td>Прямой</td>
              <td>Реверсивный</td>
            </tr>
            <tr>
              <th scope="row">Ширины в наличии</th>
              <td>{SECRET_SIZES.map(w => `${w}`).join(', ')} мм</td>
              <td>{SECRET_REVERS_SIZES.map(w => `${w}`).join(', ')} мм</td>
            </tr>
            <tr>
              <th scope="row">Высота (стандарт)</th>
              <td colspan="2" class="sd-compare__shared">{DOOR_HEIGHT} мм</td>
            </tr>
            <tr>
              <th scope="row">Направление петель</th>
              <td>Левая / Правая</td>
              <td>Левая / Правая</td>
            </tr>
            <tr>
              <th scope="row">Цвет кромки</th>
              <td colspan="2" class="sd-compare__shared">Чёрная / Серебро / Золото</td>
            </tr>
            <tr>
              <th scope="row">Покрытие полотна</th>
              <td colspan="2" class="sd-compare__shared">Финиш-грунт с кромкой</td>
            </tr>
            <tr class="sd-compare__price-row">
              <th scope="row">Полотно</th>
              <td>от {fmt(SECRET_MIN_BLADE_PRICE)}</td>
              <td>от {fmt(SECRET_REVERS_MIN_BLADE_PRICE)}</td>
            </tr>
            <tr class="sd-compare__price-row sd-compare__kit-row">
              <th scope="row">Комплект с коробом</th>
              <td><strong>от {fmt(SECRET_MIN_KIT_PRICE)}</strong></td>
              <td><strong>от {fmt(SECRET_REVERS_MIN_KIT_PRICE)}</strong></td>
            </tr>
          </tbody>
        </table>
      </div>

      <p class="sd-table-foot">
        В комплект входит: короб 54×43×2200 мм, ответная планка, 2 скрытые петли.
        Ручка, стопор и завёртка WC — опционально. Точная цена зависит от покрытия,
        цвета кромки и стороны открывания — таблица выше.
        {SECRET_PROMO_ACTIVE && ' Цены указаны с учётом акции -8% (действует до 31 августа 2026 г.).'}
      </p>

      <!-- Инфо-изображения: комплектация + сторонность — client:only="vue" (не
           client:load), см. фикс на /catalog/decor: InfoImageViewer c
           Teleport-модалкой на SSR даёт hydration mismatch (безобидно тут,
           один экземпляр, но паттерн лучше не плодить). -->
      <InfoImageViewer
        client:only="vue"
        images={[
          { src: INFO_IMAGES.kompl, caption: 'Состав комплекта', alt: 'Состав комплекта скрытой двери — профиль, петли, ответная планка' },
          { src: INFO_IMAGES.lr,    caption: 'Схема сторонности', alt: 'Схема сторонности (левая / правая) открывания скрытой двери' },
        ]}
      />

      <!-- Нестандартные высоты -->
      <div class="sd-custom-section">
        <h3 class="sd-h3">Нестандартные высоты — под заказ</h3>
        <p class="sd-p" style="margin-bottom: 1rem">
          Надбавка применяется к полотну и коробу. Максимальная высота — 2700 мм.
          Срок изготовления: {CUSTOM_LEAD_TIME}.
        </p>
        <div class="sd-table-wrap sd-stack-table-wrap">
          <table class="sd-compare sd-stack-table">
            <thead>
              <tr>
                <th scope="col">Высота</th>
                <th scope="col">Надбавка</th>
                <th scope="col">«Секрет», комплект</th>
                <th scope="col">«Секрет Реверс», комплект</th>
              </tr>
            </thead>
            <tbody>
              {CUSTOM_HEIGHT_TIERS.map(tier => (
                <tr>
                  <th scope="row">{tier.range} мм</th>
                  <td class="sd-custom__pct" data-label="Надбавка">{tier.pct}</td>
                  <td data-label="«Секрет», комплект">{fmt(calcCustomPrice(SECRET_MIN_KIT_PRICE, tier.surcharge))}</td>
                  <td data-label="«Секрет Реверс», комплект">{fmt(calcCustomPrice(SECRET_REVERS_MIN_KIT_PRICE, tier.surcharge))}</td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
        <p class="sd-table-foot" style="margin-top: 0.75rem">
          Цена включает полотно и короб с той же надбавкой. Ручка, стопор, завёртка WC — по стандартной цене.
        </p>
      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════════════
       ЦВЕТ КРОМКИ
  ══════════════════════════════════════════════ -->
  <section class="section sd-color-section">
    <div class="container">
      <div class="sd-color-wrap">
        <div class="sd-color-text">
          <h2 class="sd-h2">Цвет кромки</h2>
          <p class="sd-p">
            Кромка — алюминиевая полоска по краям проёма шириной 13 мм.
            Её видно только под углом. Цена одинакова — выбирайте
            под планируемую отделку стен и фурнитуру.
          </p>
          <p class="sd-p">
            <strong>Чёрная кромка</strong> — анодированный алюминий.
            Подходит к тёмной фурнитуре и контрастным интерьерам.
          </p>
          <p class="sd-p">
            <strong>Серебро</strong> — натуральный алюминий.
            Нейтральный вариант для светлых стен и минимализма.
          </p>
          <p class="sd-p">
            <strong>Золото</strong> — анодированный алюминий золотистого оттенка.
            Подходит к интерьерам с бронзовой или латунной фурнитурой, стилю арт-деко.
          </p>
        </div>
        <div class="sd-color-selector">
          <ColorSelector client:load />
        </div>
      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════════════
       О КОНСТРУКЦИИ
  ══════════════════════════════════════════════ -->
  <section class="section">
    <div class="container">
      <div class="sd-about">
        <div class="sd-about__text">
          <h2 class="sd-h2">О конструкции</h2>
          <p class="sd-p">
            Скрытые двери — конструкция, в которой алюминиевая коробка
            монтируется внутрь стены заподлицо с её поверхностью. Полотно
            навешивается на скрытые петли и устанавливается вровень со стеной.
            Снаружи — только тонкая кромка профиля.
          </p>
          <p class="sd-p">
            Алюминиевый профиль 54×43 мм — прочный и лёгкий. Выдерживает
            нагрузку до 70 кг. Минимальная толщина стены — 90 мм.
            Зазор между полотном и стеной — 1–2 мм.
          </p>
          <p class="sd-p">
            В ВФД скрытые двери поставляются напрямую с фабрики: замер
            по Челябинску, проект, доставка и монтаж бригадой с гарантией.
          </p>
          <div class="sd-about__actions">
            <a href={TG} target="_blank" rel="noopener" class="btn btn-primary">Написать в Telegram</a>
            <a href="/contacts/" class="btn btn-outline">Контакты</a>
            <a href={VK} target="_blank" rel="noopener" class="btn btn-outline btn-icon" aria-label="Написать в VK">
              <img src="/icons/b_vk_logo.webp" alt="" width="18" height="18" />
            </a>
            <a href={MAX} target="_blank" rel="noopener" class="btn btn-outline btn-icon" aria-label="Написать в MAX">
              <img src="/icons/b_max_logo.webp" alt="" width="18" height="18" />
            </a>
          </div>
        </div>
        <div class="sd-about__photo sd-about__photo--info">
          <img
            src={INFO_IMAGES.constr}
            alt="Конструкция скрытой двери — схема узлов монтажа"
            loading="lazy"
            decoding="async"
            width="1200"
            height="900"
          />
        </div>
      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════════════
       НАШИ РАБОТЫ
  ══════════════════════════════════════════════ -->
  {INVISIBLE_WORKS.length > 0 && (
    <section class="section" id="nashi-raboty" aria-labelledby="works-heading">
      <div class="container">
        <h2 class="sd-h2" id="works-heading">Наши работы</h2>
        <p class="sd-p" style="margin-top: -0.75rem; margin-bottom: 1.75rem">
          Реальные объекты нашей бригады в Челябинске и Челябинской области — с фото монтажа и полной спецификацией каждой двери.
        </p>
        {/* client:only="vue" — WorksGallery рендерит WorkDetailCard через v-for,
            и КАЖДАЯ карточка несёт свой <Teleport to="body"> для лайтбокса фото.
            На client:load это SSR-ится, и хайдрейшн ломает Teleport у всех
            карточек, кроме первой открытой за сессию: клик по фото 2-й/3-й/4-й
            работы просто ничего не делает (TypeError: insertBefore на null),
            без видимой ошибки для пользователя — баг был живым на проде. */}
        <WorksGallery client:only="vue" works={INVISIBLE_WORKS} tgHref={TG} limit={WORKS_LIMIT} archiveHref={ARCHIVE_HREF} />
      </div>
    </section>
  )}

  <!-- ══════════════════════════════════════════════
       FAQ
  ══════════════════════════════════════════════ -->
  <section class="section">
    <div class="container">
      <FaqAccordion items={faq} />
    </div>
  </section>

  <!-- ══════════════════════════════════════════════
       CTA
  ══════════════════════════════════════════════ -->
  <section class="section">
    <div class="container">
      <div class="sd-cta">
        <div class="sd-cta__text">
          <h2 class="sd-cta__h2">Рассчитаем бесплатно</h2>
          <p class="sd-cta__lead">
            Замер по Челябинску, точная стоимость, подбор фурнитуры.
            Двери напрямую с фабрики.
          </p>
        </div>
        <div class="sd-cta__actions">
          <a href={TG} target="_blank" rel="noopener" class="btn btn-ghost">Написать в Telegram</a>
          <a href={`tel:${PHONE}`} class="btn btn-ghost">Позвонить</a>
          <a href={VK} target="_blank" rel="noopener" class="btn btn-ghost btn-icon" aria-label="Написать в VK">
            <img src="/icons/w_vk.webp" alt="" width="18" height="18" />
          </a>
          <a href={MAX} target="_blank" rel="noopener" class="btn btn-ghost btn-icon" aria-label="Написать в MAX">
            <img src="/icons/w_max.webp" alt="" width="18" height="18" />
          </a>
        </div>
      </div>

      <nav class="sd-related" aria-label="Смежные разделы">
        <a href="/catalog/">Каталог дверей</a>
        <a href="/partitions/">Перегородки</a>
        <a href="/contacts/">Контакты</a>
      </nav>
    </div>
  </section>

</BaseLayout>

<style>
/* ── Globals ─────────────────────────────────────────────────── */
.sd-eyebrow {
  display: block;
  margin: 0 0 0.875rem;
  font-size: 0.6875rem;
  font-weight: 600;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: rgba(255,255,255,0.55);
}
.sd-h2 {
  margin: 0 0 1.5rem;
  font-size: clamp(1.625rem, 3vw, 2.125rem);
  font-weight: 500;
  color: #0f172a;
  letter-spacing: -0.02em;
  line-height: 1.15;
}
.sd-p {
  margin: 0 0 1rem;
  color: #64748b;
  font-size: clamp(0.9375rem, 1.4vw, 1.0625rem);
  line-height: 1.75;
}

/* ── Hero ─────────────────────────────────────────────────────── */
.sd-hero-wrap { padding-top: clamp(0.75rem, 1.5vw, 1.25rem); padding-bottom: clamp(2.5rem, 4vw, 3.5rem); }
.sd-hero {
  position: relative;
  overflow: hidden;
  border-radius: 1.5rem;
  min-height: clamp(52vh, 72vw, 80vh);
  display: flex;
  align-items: flex-end;
  background: #0f172a;
}
.sd-hero__body {
  position: relative;
  z-index: 1;
  padding: clamp(2rem, 5vw, 4rem);
  color: #fff;
  max-width: 44rem;
}
.sd-hero__h1 {
  margin: 0 0 1rem;
  font-size: clamp(2.5rem, 6vw, 4rem);
  font-weight: 500;
  line-height: 1.02;
  letter-spacing: -0.03em;
  text-shadow: 0 2px 16px rgba(0,0,0,0.35);
}
.sd-hero__lead {
  margin: 0 0 2rem;
  max-width: 38rem;
  font-size: clamp(1rem, 1.6vw, 1.125rem);
  line-height: 1.65;
  color: rgba(255,255,255,0.88);
  text-shadow: 0 1px 8px rgba(0,0,0,0.3);
}
.sd-hero__actions { display: flex; flex-wrap: wrap; gap: 0.625rem; }

/* ── Products ─────────────────────────────────────────────────── */
.sd-products { display: flex; flex-direction: column; gap: clamp(1rem, 2.5vw, 1.5rem); }
.sd-product {
  display: grid;
  grid-template-columns: 1fr 1fr;
  border: 1px solid #e5e7eb;
  border-radius: 1.25rem;
  overflow: hidden;
}
/* Реверс: фото справа */
.sd-product--rev { direction: rtl; }
.sd-product--rev > * { direction: ltr; }

.sd-product__photo {
  position: relative;
  overflow: hidden;
  background: #f8fafc;
  min-height: 28rem;
}
.sd-product__photo img {
  display: block;
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform 450ms ease;
}
.sd-product:hover .sd-product__photo img { transform: scale(1.03); }

.sd-product__info {
  padding: clamp(2rem, 4vw, 3.5rem);
  display: flex;
  flex-direction: column;
}
.sd-product__eyebrow {
  font-size: 0.6875rem;
  font-weight: 600;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: #94a3b8;
  margin-bottom: 0.375rem;
}
.sd-product__name {
  margin: 0 0 0.25rem;
  font-size: clamp(1.5rem, 3vw, 2rem);
  font-weight: 500;
  color: #0f172a;
  letter-spacing: -0.02em;
}
.sd-product__type {
  margin: 0 0 1.25rem;
  font-size: 0.875rem;
  font-weight: 600;
  color: #64748b;
  text-transform: uppercase;
  letter-spacing: 0.08em;
}
.sd-product__desc {
  margin: 0 0 1.5rem;
  color: #64748b;
  font-size: 0.9375rem;
  line-height: 1.7;
  flex: 1;
}
.sd-product__specs {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
  margin-bottom: 1.75rem;
  padding: 1.25rem;
  border-radius: 0.75rem;
  background: #f8fafc;
  border: 1px solid #f1f5f9;
}
.sd-spec { display: flex; align-items: baseline; gap: 0.625rem; }
.sd-spec__key {
  font-size: 0.8125rem;
  font-weight: 600;
  color: #94a3b8;
  white-space: nowrap;
  flex-shrink: 0;
  min-width: 9rem;
}
.sd-spec__val {
  font-size: 0.9375rem;
  font-weight: 600;
  color: #334155;
}
.sd-product__price {
  display: flex;
  gap: 1.5rem;
  margin-bottom: 1.75rem;
  flex-wrap: wrap;
}
.sd-product__price > div { display: flex; flex-direction: column; gap: 0.25rem; }
.sd-price-label {
  font-size: 0.6875rem;
  font-weight: 600;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: #94a3b8;
}
.sd-price-val {
  font-size: 1.375rem;
  font-weight: 500;
  color: #0f172a;
  letter-spacing: -0.01em;
}
.sd-price-val--accent { color: #0f172a; }
.sd-price-old {
  margin-right: 0.375rem;
  font-size: 0.9375rem;
  font-weight: 500;
  color: #475569;
  text-decoration: line-through;
  text-decoration-color: #cbd5e1;
}
.sd-promo-note {
  margin: 0.75rem 0 1rem;
  padding: 0.75rem 1rem;
  border-radius: 0.75rem;
  background: #fef2f2;
  color: #b91c1c;
  font-size: 0.875rem;
  font-weight: 600;
}
.sd-promo-chip {
  display: inline-flex;
  align-self: flex-start;
  align-items: center;
  gap: 0.375rem;
  margin-bottom: 1rem;
  padding: 0.3rem 0.75rem;
  border-radius: 9999px;
  background: #fee2e2;
  color: #b91c1c;
  font-size: 0.75rem;
  font-weight: 700;
}
.sd-product__btn { align-self: flex-start; }

/* ── H3 ───────────────────────────────────────────────────────── */
.sd-h3 {
  margin: 0 0 0.75rem;
  font-size: clamp(1.125rem, 2vw, 1.375rem);
  font-weight: 500;
  color: #0f172a;
  letter-spacing: -0.015em;
  line-height: 1.2;
}


/* ── Custom heights section ───────────────────────────────────── */
.sd-custom-section {
  margin-top: 2.5rem;
  padding-top: 2rem;
  border-top: 1px solid #f1f5f9;
}
.sd-custom__pct {
  font-weight: 500 !important;
  color: #0f172a !important;
}

/* ── Compare table ────────────────────────────────────────────── */
.sd-table-wrap {
  overflow-x: auto;
  -webkit-overflow-scrolling: touch;
  border: 1px solid #e5e7eb;
  border-radius: 1rem;
  margin-bottom: 1rem;
}
.sd-compare {
  width: 100%;
  border-collapse: collapse;
  font-size: 0.9375rem;
  background: #fff;
}
.sd-compare thead tr {
  border-bottom: 2px solid #0f172a;
}
.sd-compare thead th {
  padding: 1rem 1.375rem;
  text-align: left;
  font-size: 1rem;
  font-weight: 500;
  color: #0f172a;
}
.sd-compare thead .sd-compare__row-head { color: transparent; }
.sd-compare tbody th {
  padding: 0.875rem 1.375rem;
  text-align: left;
  font-size: 0.8125rem;
  font-weight: 600;
  color: #94a3b8;
  text-transform: uppercase;
  letter-spacing: 0.06em;
  white-space: nowrap;
  background: #fafbfc;
  border-right: 1px solid #f1f5f9;
  width: 40%;
}
.sd-compare tbody td {
  padding: 0.875rem 1.375rem;
  color: #334155;
  font-weight: 600;
  border-bottom: 1px solid #f8fafc;
}
.sd-compare tbody tr:last-child td,
.sd-compare tbody tr:last-child th { border-bottom: none; }
.sd-compare__shared { font-style: normal; }
.sd-compare__price-row th,
.sd-compare__price-row td { padding-top: 1rem; }
.sd-compare__price-row th { color: #64748b; }
.sd-compare__kit-row { border-top: 1px solid #e5e7eb !important; }
.sd-compare__kit-row th { font-size: 0.9375rem; font-weight: 600; color: #0f172a; text-transform: none; letter-spacing: 0; }
.sd-compare__kit-row td { font-size: 1.125rem; }
.sd-compare__kit-row strong { color: #0f172a; font-weight: 500; }

.sd-table-foot {
  margin: 0;
  font-size: 0.8125rem;
  color: #94a3b8;
  line-height: 1.6;
}

/* ── Короб «Секрет»: Стандарт / Лайт — мобайл-фёрст (база — 1 колонка,
   2 колонки только от планшета), т.к. это длинный editorial-контент. ── */
.sd-box-types {
  display: grid;
  grid-template-columns: 1fr;
  gap: 2rem;
  margin-top: 1.5rem;
}
.sd-box-type__name {
  margin: 0 0 0.625rem;
  font-size: 1.0625rem;
  font-weight: 500;
  color: #0f172a;
  letter-spacing: -0.01em;
}
.sd-box-feature {
  margin-top: 1.125rem;
  padding-top: 1.125rem;
  border-top: 1px solid #f1f5f9;
}
.sd-box-feature h5 {
  margin: 0 0 0.375rem;
  font-size: 0.9375rem;
  font-weight: 600;
  color: #1e293b;
}
.sd-box-feature p {
  margin: 0;
  color: #64748b;
  font-size: 0.9375rem;
  line-height: 1.7;
}
@media (min-width: 720px) {
  .sd-box-types { grid-template-columns: 1fr 1fr; gap: clamp(1.5rem, 3vw, 2.5rem); }
}

/* ── Color section ────────────────────────────────────────────── */
.sd-color-section { border-top: 1px solid #f1f5f9; border-bottom: 1px solid #f1f5f9; }
.sd-color-wrap {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: clamp(2rem, 5vw, 4.5rem);
  align-items: center;
}
.sd-color-selector { display: flex; justify-content: center; }

/* ── About ────────────────────────────────────────────────────── */
.sd-about {
  display: grid;
  grid-template-columns: 1.1fr 1fr;
  gap: clamp(2rem, 5vw, 4.5rem);
  align-items: center;
}
.sd-about__actions { display: flex; flex-wrap: wrap; gap: 0.625rem; margin-top: 1.5rem; }
.sd-about__photo {
  border-radius: 1.25rem;
  overflow: hidden;
  border: 1px solid #e5e7eb;
}
.sd-about__photo > img {
  display: block;
  width: 100%;
  aspect-ratio: 4/5;
  object-fit: cover;
}
.sd-about__photo--info > img {
  aspect-ratio: auto;
  max-height: 32rem;
  object-fit: contain;
  background: #f8fafc;
  padding: 1.5rem;
}

/* ── CTA ──────────────────────────────────────────────────────── */
.sd-cta {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 2rem;
  flex-wrap: wrap;
  padding: clamp(2.5rem, 5vw, 4rem);
  border-radius: 1.25rem;
  background: #212124;
  color: #fff;
}
.sd-cta__h2 {
  margin: 0 0 0.5rem;
  font-size: clamp(1.5rem, 3vw, 2.125rem);
  font-weight: 500;
  letter-spacing: -0.02em;
}
.sd-cta__lead {
  margin: 0;
  font-size: 0.9375rem;
  color: rgba(255,255,255,0.6);
  line-height: 1.6;
}
.sd-cta__actions { display: flex; gap: 0.625rem; flex-wrap: wrap; flex-shrink: 0; }

/* ── Related ──────────────────────────────────────────────────── */
.sd-related {
  display: flex;
  flex-wrap: wrap;
  gap: 0.625rem;
  justify-content: center;
  margin-top: 1.5rem;
}
.sd-related > a {
  font-size: 0.8125rem;
  font-weight: 600;
  color: #64748b;
  text-decoration: none;
  padding: 0.4rem 0.875rem;
  border-radius: 9999px;
  border: 1px solid #e5e7eb;
  transition: color 150ms ease, border-color 150ms ease;
}
.sd-related > a:hover { color: #0f172a; border-color: #94a3b8; }

/* ── Responsive (mobile-first уточнения) ─────────────────────── */
@media (max-width: 900px) {
  .sd-color-wrap { grid-template-columns: 1fr; }
  .sd-about       { grid-template-columns: 1fr; }
  .sd-about__photo { order: -1; }
  .sd-about__photo--info > img { max-height: 26rem; }
}
@media (max-width: 768px) {
  .sd-product { grid-template-columns: 1fr; direction: ltr; }
  .sd-product--rev { direction: ltr; }
  .sd-product__photo { min-height: 16rem; }
  .sd-product__info { padding: 1.75rem 1.5rem; }
  .sd-cta { flex-direction: column; align-items: flex-start; gap: 1.5rem; padding: 1.75rem 1.5rem; }
  .sd-product__price { gap: 1.25rem; }
}
@media (max-width: 640px) {
  /* Hero: переключатель стилей идёт первым элементом потока (см. CoverSwitcher.vue),
     поэтому секция должна стать column-flex — иначе он стоит вторым в row-направлении
     и сжимается до нулевой ширины. Заголовок остаётся прижатым к низу карточки. */
  .sd-hero {
    flex-direction: column;
    align-items: stretch;
    justify-content: space-between;
  }
}
@media (max-width: 560px) {
  /* Спеки — в столбик */
  .sd-spec { flex-direction: column; gap: 0.125rem; }
  .sd-spec__key { min-width: 0; }

  /* Таблицы: компактнее ячейки, мельче шрифт, перенос заголовков строк */
  .sd-compare { font-size: 0.8125rem; }
  .sd-compare thead th { padding: 0.7rem 0.75rem; font-size: 0.875rem; }
  .sd-compare tbody th {
    padding: 0.7rem 0.75rem;
    width: auto;
    min-width: 5.5rem;
    white-space: normal;
  }
  .sd-compare tbody td { padding: 0.7rem 0.75rem; }
  .sd-compare__kit-row td { font-size: 1rem; }

  /* Заголовок нестандарта — плотнее */
  .sd-custom-section { margin-top: 1.75rem; padding-top: 1.5rem; }

  /* Hero — плотнее лид, инфо-фото конструкции ниже */
  .sd-hero__lead { margin-bottom: 1.5rem; }
  .sd-about__photo--info > img { max-height: 20rem; padding: 1rem; }
  .sd-h2 { margin-bottom: 1rem; }

  /* Кнопки в карточке товара — на всю ширину для удобного тапа */
  .sd-product__btn { align-self: stretch; }
}

/* Таблицы цен «Секрет» и «Рефлекс» по кромке/стороне — 5-6 колонок,
   горизонтальный скролл технически работает, но незаметен (обрезанная
   последняя колонка выглядит как баг, а не как «свайпни вправо»).
   На узких экранах — карточками, по одной строке на комбинацию,
   тот же приём, что на /catalog/decor. */
@media (max-width: 640px) {
  .sd-stack-table-wrap { overflow-x: visible; border: none; border-radius: 0; margin-top: 1.25rem; }
  .sd-stack-table, .sd-stack-table thead, .sd-stack-table tbody, .sd-stack-table tr, .sd-stack-table th, .sd-stack-table td {
    display: block;
    width: 100%;
  }
  .sd-stack-table thead { display: none; }
  .sd-stack-table tbody tr {
    padding: 1rem 1.125rem;
    margin-bottom: 0.75rem;
    border: 1px solid #e5e7eb;
    border-radius: 0.875rem;
    background: #fff;
  }
  .sd-stack-table tbody tr:last-child { margin-bottom: 0; }
  .sd-stack-table tbody td {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 1rem;
    padding: 0.375rem 0;
    border-bottom: none;
    white-space: normal;
  }
  .sd-stack-table tbody td::before {
    content: attr(data-label);
    color: #94a3b8;
    font-weight: 500;
    font-size: 0.8125rem;
    flex-shrink: 0;
  }
  /* Строки с th scope="row" (напр. высота в тирах нестандартных размеров) —
     заголовок карточки, а не пара label:value. */
  .sd-stack-table tbody th {
    text-align: left;
    padding: 0 0 0.5rem;
    margin-bottom: 0.375rem;
    border-bottom: 1px solid #f1f5f9;
    font-size: 0.9375rem;
    font-weight: 600;
    color: #0f172a;
  }
}
</style>
